apiVersion: v1
kind: Service
metadata:
  name: student-task-manager-service
spec:
  type: NodePort
  selector:
    app: student-task-manager
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080